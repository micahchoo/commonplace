{
  "name": "arenotebook",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A self-hostable static site that browses Are.na public channels.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "svelte-check --tsconfig ./jsconfig.json || svelte-check"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "jsdom": "^26.0.0",
    "svelte": "^5.19.0",
    "svelte-check": "^4.1.4",
    "vite": "^6.0.7",
    "vitest": "^3.0.2"
  },
  "dependencies": {
    "dompurify": "^3.2.4"
  }
}
