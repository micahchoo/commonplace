{"id":"binder-9030","title":"Binder × Are.na: modernize into an Are.na-driven static site","status":"open","type":"epic","priority":2,"createdAt":"2026-07-10T04:04:20.563Z","updatedAt":"2026-07-11T22:02:21.460Z","description":"## Notes\n\n**Domain.** Binder is a self-hostable static single-page site: it renders a navigable menu and loads whichever entry you pick into a full-viewport `<iframe>`. Today the menu comes from a hand-edited `info.json` (`title` / `about` / `logo` / a `menu` of name→URL) and the engine is ~90 lines of jQuery (`js/google-docs-site.1.0.js`) plus jQuery-UI + touch-punch + modernizr. This effort re-sources content from **Are.na** and modernizes the stack, while staying static and self-hostable.\n\n**Standing decisions (from the charting grill + since).**\n- **Name:** the rebuilt tool is **AreNotebook** (Are.na + notebook) — see *Name the rebuilt tool*. Caveat flagged (brand-association/trademark) and reversible; repo/docs still say \"binder\" until the rebrand lands with migration.\n- **Data:** runtime client-side `fetch` of the Are.na **public V3 REST API** — no auth (public reads are CORS-enabled), no build-time data bake. Mind V3's **guest rate limit (30/min)** → cache + lazy pagination + `429` backoff.\n- **Stack:** **Svelte 5 (runes) + Vite**, static output — see *Choose the framework and build setup*.\n- **Are.na's role:** a **richer organizing layer** — reimagine navigation around Are.na's structure (block types, nested channels, connections), not a 1:1 of today's flat menu.\n- **Config:** a runtime-fetched `config.json` (`channels[]` of slugs + `title/about/logo/theme`), signature look default + tokenized — see *Design the self-hosting config*.\n- **Constraint:** self-hostable as plain static files; no server backend; no secrets in the deploy.\n\n**Skills each session should consult.** `/grilling` + `/domain-modeling` (default), `/prototype` (design tickets), `/research` (API tickets), `/brainstorming` (naming). Tracker conventions live in `docs/agents/issue-tracker.md`.\n\n**Code touchpoints.** `index.html` (iframe host + menu markup), `js/google-docs-site.1.0.js` (the engine to replace), `style.css` (signature aesthetic — box-shadow menu, monospace — to preserve or rethink), `info.json` (the config Are.na supersedes).\n\n## Decisions so far\n\n<!-- one line per closed ticket: gist + pointer -->\n\n- **Research the Are.na public API** (`binder-b6d4`) — Are.na is public/no-auth and CORS `*`, so runtime browser fetch works with no proxy. Six block classes mapped; nested channels via `/connections`. (V2-verified; see the V3 decision for the API version adopted.) → `docs/research/arena-public-api.md`\n- **Sketch a wireframe-storyboard** (`binder-e3d6`) — Chose the **\"Same Box, Deeper Stack\"** direction: preserve both signatures (draggable box-shadow menu + full-viewport background); per-class renderer in the single slot; in-place breadcrumb drill; connections band; opt-in Board; active row **black**. → `docs/design/binder-arena-storyboard.md`\n- **Design the channel-to-navigation organizing model** (`binder-7ac4`) — Site = **several top-level channels (\"sections\")**; each = ordered `available` blocks + nested Channel drill-nodes + a connections strip; uniform breadcrumb drill (cap 8 + cycle guard); per-class renderer; draggable panel preserved, logo/about folded in; **hash-routing kept** (deep-link by block id); **no fetch-all** (page-1-free + lazy). → `docs/design/organizing-model.md`\n- **Decide per-block embedding & the non-iframeable fallback** (`binder-ab29`) — Per-class renderers (Image→`<img>`, Text→DOMPurify'd html, Media/Embed→sandboxed `srcdoc`, Attachment→PDF iframe desktop / download card mobile, Link→iframe unless denylisted). A static app **can't** detect a framing refusal, so: a shipped **hostname denylist** (NYT/Twitter/FB/GitHub verified `XFO DENY`) + always-present \"open in new tab\" escape + fallback card. → `docs/design/embedding.md`\n- **Choose the framework and build setup** (`binder-c14b`) — **Svelte 5 (runes) + Vite**, static SPA. Chosen for reactivity/scoped-styling fit; blind 5-way eval edged Preact on size (53 vs 49), delta negligible self-hosted. Gotchas noted (scoped styles miss `{@html}`; use `SvelteMap`; hand-roll routing). → `docs/design/framework.md`\n- **Evaluate Are.na API V3 vs deprecated V2** (`binder-d4d4`) — **Build on V3.** Live, public-read, CORS `*`, no auth (verified); V2 deprecated (no sunset). Delta mechanical (`class`→`type`, `Media`→`Embed`, `content_html`→`content.html`; `embed.html`/`source.url`/`attachment.url` unchanged; `{data,meta}` pagination). V3 **publishes rate limits** (guest 30/min + headers + `429`). → `docs/research/arena-api-v3.md`\n- **Design the self-hosting config & theming** (`binder-5299`) — A runtime-fetched **`config.json`** (no rebuild) + `?channel=` URL override; schema `{title?, about?, logo?, channels[], theme?}`, no token (public-only). **Theming: signature look default, fully tokenized** via CSS custom properties in a global stylesheet. Migration: `info.json`→`config.json` (`menu`→`channels` is a content-model shift). → `docs/design/config.md`\n- **Name the rebuilt tool** (`binder-c8c3`) — **AreNotebook** (Are.na + notebook). Availability fully clear (npm + gh-org + `.com`/`.io` all free). Caveat flagged: leans on the Are.na brand (trademark/association) + mild say-ability — reversible, worth a sanity-check before the rebrand. → `docs/design/name.md`\n\n## Fog\n\n- **Codebase migration** — targets **Svelte 5 + Vite** on **Are.na V3**: scaffold the app, port the (path-encoding) hash-routing + the iframe host, wire the per-class renderers + DOMPurify + sandbox, load `config.json`, reimplement the draggable panel natively, drop jQuery / jQuery-UI / touch-punch / modernizr / the legacy GA snippet. Translate the design docs' V2 field names to V3 per `docs/research/arena-api-v3.md`. **Rebrand** Binder→AreNotebook (repo, docs, README, `binder-clip` assets).\n- **Runtime resilience** — honor V3's `X-RateLimit-*` + `429` backoff; handle outages and private/deleted channels; lazy pagination + per-section degrade + in-session cache.\n- **Board view (deferred)** — the opt-in thumbnail grid for large/image-heavy channels, once the base list nav ships.\n- **Deployment story** — the static `dist/` drops onto any static host (GitHub Pages / Netlify / plain nginx); confirm the self-hoster's build/deploy path.","labels":["wayfinder:map"]}
{"id":"binder-b6d4","title":"Research the Are.na public API: channels, blocks, embedding & CORS","status":"closed","type":"task","priority":0,"createdAt":"2026-07-10T04:04:20.628Z","updatedAt":"2026-07-11T19:50:30.816Z","description":"## Question\n\nWhat does the Are.na **public** REST API expose for reading a channel, and can a browser use it directly? Establish the facts the rest of the map depends on:\n\n- Endpoints to read a public channel and its contents (`GET /channels/:slug`, `/channels/:slug/contents`), pagination (`page` / `per`), and rate limits.\n- The **block types** (Link, Image, Text, Media/embed, Attachment, Channel-connection, …) and, for each, the fields that yield a URL / embed HTML / thumbnail.\n- **CORS:** does the API send headers allowing browser-side `fetch` from an arbitrary static origin, with no proxy? (Load-bearing for the runtime-fetch decision.)\n- How **nested channels** and **connections** are represented (the raw material for the \"richer organizing layer\").\n\nDeliver a markdown summary as a linked asset. Use `/research`.\n\n## Answer\n\nFull write-up: **`docs/research/arena-public-api.md`** (verified live against `api.are.na/v2` on 2026-07-11 + V2 docs).\n\n- **Runtime browser fetch works, no proxy.** Public *and* \"closed\" channels read with **no auth**; the API returns **`Access-Control-Allow-Origin: *`** on GET and OPTIONS preflight. Green-lights the runtime-fetch decision. Only *private* channels need a token (OAuth code/token flow — unsafe for a static site; confirms public-only).\n- **Endpoints:** primary read is `GET /channels/:slug?per=100&page=K` (returns channel meta + `length` + a page of `contents`). `/contents` is lighter but omits `length`. Nested channels via `GET /channels/:slug/connections`. **`per` caps at 100** → fetch-all = `ceil(length/100)` pages.\n- **Six block classes**, fields verified: **Link** (`source.url` → external site, iframe target), **Media** (`embed.html` = embedly iframe → framing-safe for YouTube/Vimeo/Bandcamp), **Image** (`image.*.url` → `<img>`), **Attachment** (`attachment.url` + `content_type`, e.g. PDF), **Text** (`content_html` inline), **Channel** (nested connection: `slug`/`title`/`length`). `generated_title` is a ready nav label; filter `state == \"Available\"`.\n- **Framing constraint confirmed:** Link blocks hit arbitrary sites, many of which refuse iframes (`X-Frame-Options`/CSP — NYT/Twitter/FB/GitHub per the README). Media dodges it via embedly. → feeds `binder-ab29`.\n- **Risks:** V2 is **deprecated** (V3 docs login-gated, unverified) → new ticket to evaluate V3; and **no rate-limit headers** are exposed → be a courteous runtime client (feeds the \"runtime resilience\" fog).\n\nCascade: closing this unblocks `binder-7ac4` (organizing model) and `binder-ab29` (embedding).","labels":["wayfinder:ticket","wayfinder:research","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T19:01:08.227Z","closeReason":"V2 public API: no-auth + CORS * (runtime fetch works, no proxy); 6 block classes mapped; nested channels via /connections; per caps at 100. Risks: V2 deprecated, no rate-limit headers. Asset: docs/research/arena-public-api.md"}
{"id":"binder-c14b","title":"Choose the framework and build setup","status":"closed","type":"task","priority":1,"createdAt":"2026-07-10T04:04:20.693Z","updatedAt":"2026-07-11T21:17:55.980Z","description":"## Question\n\nWhich light framework + build setup should the modernized Binder use? It is a small, runtime-fetch, static-output app — essentially an iframe host with a dynamic (and, per the organizing-layer decision, likely interactive / nested) navigation.\n\nCompare candidates — Svelte, Preact, Astro, SolidJS, Lit — against: runtime size, quality of **static** output, DX / build simplicity (Vite), and fit for a reactive nested nav. Recommend one with rationale.\n\nNote the interaction with *Design the channel-to-navigation organizing model* (a richer nav may favor a reactive framework), but this is decidable now on general criteria. Use `/grilling`.\n\n## Answer\n\nDecision: **Svelte 5 (runes) + Vite**, static SPA output. Full writeup: **`docs/design/framework.md`**.\n\n- **Chosen by the maintainer** for authoring ergonomics + reactivity/scoped-styling fit (9/9 on reactivity and styling for Binder's nested nav + signature panel).\n- **Honest tradeoff:** a blind 5-way eval (scored /60) ranked **Preact 53, Solid 51, Lit 51, Svelte 49, Astro 37** — Preact edged it on size (Svelte's ~10 kB gzip runes floor vs ~6 kB) and contributor pool. The ~4 kB gzip delta is negligible for a self-hosted static app, and the judge endorsed \"if the team lives in Svelte, take Svelte.\"\n- **Framework-neutral:** the drag = native Pointer Events, hash-routing = native `onhashchange`, DOMPurify = a shared ~10 kB cost — none of Binder's distinctive features hinge on the pick.\n- **Gotchas to design around:** scoped styles don't reach `{@html}` (style Text/Media renderers via `:global()`); use `SvelteMap` for the channel cache; hand-roll hash routing (not SvelteKit); the skeleton box covers client-render first paint.\n- **Build:** `@sveltejs/vite-plugin-svelte`; `vite build` → static `dist/`; drop jQuery/jQuery-UI/touch-punch/modernizr.\n\nUnblocks the codebase-migration and deployment fog (migration also waits on config, `binder-5299`). No new ticket.","labels":["wayfinder:ticket","wayfinder:grilling","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T21:17:55.980Z","closeReason":"Decided: Svelte 5 (runes) + Vite, static SPA. Chosen for reactivity/styling fit + ergonomics; blind 5-way eval edged Preact on size (53 vs 49) but delta negligible self-hosted. Gotchas captured. docs/design/framework.md"}
{"id":"binder-7ac4","title":"Design the channel-to-navigation organizing model","status":"closed","type":"task","priority":0,"createdAt":"2026-07-10T04:04:20.756Z","updatedAt":"2026-07-11T20:44:11.337Z","description":"## Question\n\nDesign how an Are.na channel's structure maps to Binder's navigation and embedding — the \"richer organizing layer.\" Given the real API shape (see *Research the Are.na public API*):\n\n- What is a nav entry — a block? a channel? a connection?\n- How do **nested channels** and **connections** surface — nested menu, sections, a graph?\n- How do different **block types** behave when selected — iframe, embed, inline?\n- What happens to the current signature UX — the draggable movable menu, the full-viewport iframe, the `.about` text?\n\nMake it concrete with a `/prototype` (outline or stub UI) to react to. Blocked until the API facts land.\n\n## Answer\n\nDecided model: **`docs/design/organizing-model.md`**. Settled via grilling (4 forks) + an adversarial critic pass that caught real gaps, all folded in.\n\n- **Root = SEVERAL top-level channels (\"sections\").** Each section = a channel of ordered `available` blocks + nested `Channel` drill-nodes + a connections strip; the root is a *synthetic* channel, so the whole tree is uniform.\n- **A nav entry** = a block (by `position`, `generated_title` label, `class` tag). `Channel` blocks (in `contents`) = drill nodes; `/connections` (items carry `slug`) = sideways jumps.\n- **Block behavior** = a per-class renderer in one full-viewport slot: Link→iframe *(framing-safe only; fallback → `binder-ab29`)*, Media→`embed.html`, Image→`<img>`, Text→`content_html`, Attachment→PDF.\n- **Signature UX preserved:** draggable box-shadow panel + full-viewport slot; logo/about **folded into the panel header**; active row **black**.\n- **Hardened after critique:** keep **hash-routing** (deep-link by stable block **id**, not ordinal; landing defers to the hash); a **connection jump = a fresh breadcrumb rooted at the target** (strip hides in-path channels); **no fetch-all** (page 1 comes free with the channel fetch, then lazy-load); filter `state==available`; **per-section degrade** for dead slugs; **depth cap 8 + cycle guard**.\n\nForks decided: connections = nested + strip (not a full graph); root = several channels; logo/about folded in; Board deferred.\n\nGraduates a **self-hosting-config** ticket (config shape + delivery + `info.json` migration). Board stays fog. Does not decide embedding fallbacks (`binder-ab29`) or the framework (`binder-c14b`).","labels":["wayfinder:ticket","wayfinder:prototype","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T20:44:11.337Z","closeReason":"Organizing model decided (docs/design/organizing-model.md): site=several top-level channels; per-class renderer in one slot; uniform breadcrumb drill (cap8+cycle guard); hash-routing kept; no fetch-all. Grilled + adversarially hardened. Graduated a config ticket."}
{"id":"binder-ab29","title":"Decide per-block embedding and the non-iframeable fallback","status":"closed","type":"task","priority":1,"createdAt":"2026-07-10T04:04:20.818Z","updatedAt":"2026-07-11T21:01:02.552Z","description":"## Question\n\nDecide how each Are.na block type is embedded, and the fallback when a URL refuses framing. Today everything loads in one `<iframe>`, but many sites reject it (the README already lists NYT / Twitter / Facebook / GitHub — X-Frame-Options / CSP).\n\nGiven the API's block types (see *Research the Are.na public API*): map each type to a render strategy (iframe / oEmbed embed / native `<img>` / inline text / attachment viewer), detect non-iframeable URLs, and specify the fallback UX (open-in-new-tab, thumbnail + link, reader view). Blocked until the API facts land.\n\n## Answer\n\nDecided: **`docs/design/embedding.md`**. Grilled inputs + empirical header probes (2026-07-11) + two adversarial critic passes.\n\n- **Per-class renderers** in the one slot: Image→`<img>` (srcset); Text→**DOMPurify**-sanitized `content_html`; Media→**one sandboxed `<iframe srcdoc=\"{embed.html}\">`** (handles both `video` *and* `rich`/script embeds; `allow=` preserves fullscreen/autoplay); Attachment→PDF iframe on **desktop** / download card on **mobile** (attachments.are.na verified: `application/pdf`, no XFO, no `Content-Disposition`; `video/*`→`<video>`, `audio/*`→`<audio>`); Link→iframe `source.url` unless denylisted.\n- **Crux:** a static app **cannot** detect or pre-check a framing refusal — no `error` event; `load` fires even when blocked; CORS blocks a header pre-check; no backend. So: a shipped **hostname-matched denylist** (evidence-seeded — NYT/Twitter/FB/GitHub confirmed `X-Frame-Options: DENY`) + an **always-present \"open in new tab ▸\"** escape hatch (time-based emphasis, not `onload`). The fallback **card** (`image.thumb` + provider + title + open-in-tab) is the *common* outcome for social/news/platform Links; framing is for the long tail.\n- **Security:** sandbox framed content (no `allow-top-navigation`), `strict-origin-when-cross-origin` referrer, lazy; the Media `srcdoc` isolates third-party JS in a null origin; the Attachment PDF iframe is **not** sandboxed (Chrome's viewer blanks under it).\n- Reconciled `organizing-model.md` (Media wording + landing \"prefer a class that paints\").\n\nHands the DOMPurify/sandbox wiring to `binder-c14b` (framework). No new ticket — the denylist is a maintained in-app file.","labels":["wayfinder:ticket","wayfinder:research","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T21:01:02.552Z","closeReason":"Embedding decided (docs/design/embedding.md): per-class renderers; Media=sandboxed srcdoc embed; static app can't detect framing refusal -> shipped hostname denylist + open-in-new-tab escape + fallback card. Empirically header-probed + 2x adversarial critic."}
{"id":"binder-d4d4","title":"Evaluate Are.na API V3 vs staying on deprecated V2","status":"closed","type":"task","priority":2,"createdAt":"2026-07-11T19:01:08.339Z","updatedAt":"2026-07-11T21:32:59.309Z","description":"## Question\n\nEvaluate Are.na API **V3** versus staying on the deprecated **V2**. V2 (`api.are.na/v2`) is what Binder targets today — public/no-auth, CORS `*`, empirically verified working — but the V2 docs are banner-marked **deprecated** and point to a V3 at `are.na/developers` whose reference pages are **login-gated** (unverified during the API research).\n\nDetermine: V3's base URL and public-channel read endpoints; whether V3 supports **unauthenticated public-channel reads with CORS**; any announced **V2 sunset** timeline; the migration delta. Recommend **V2-now-behind-an-adapter** vs **V3-now**. Use `/research`.\n\n## Answer\n\nRecommendation: **build on V3 now** (reverses the earlier V2-adapter lean). Full writeup: **`docs/research/arena-api-v3.md`**. Empirically probed live + `are.na/developers/explore` (2026-07-11).\n\n- **V3 is live, public-read, CORS `*`, no auth** — verified (`GET /v3/channels/:slug` → 200, `ACAO: *`; reads are \"Optional\"-auth). It's the actively-documented going-forward REST API; **V2 is deprecated (no sunset date published)** — building a new app on it is a false economy.\n- **Migration delta is mechanical, not a redesign:** `class`→`type`, `Media`→`Embed`, `content_html`→`content.html`, `image.display.url`→`image.large`; **`embed.html` / `source.url` / `attachment.url` unchanged**. `generated_title` gone (derive the label); `position` moved into the per-block `connection` object; contents via `{data, meta}` (`has_more_pages`); `visibility` replaces `status`; `counts` replaces `length`. Full table in the doc.\n- **New: V3 publishes & enforces rate limits** — guest **30/min**, `X-RateLimit-*` headers, `429`. This constrains *and* helps: it validates the lazy/cached/no-fetch-all decisions and adds a concrete **429-backoff** requirement. 30/min is fine per-user with caching; a token (Free 120) is a power-user toggle (same secret-exposure tradeoff → default guest).\n\nConsequences: standing **Data** decision → V3; `organizing-model.md` + `embedding.md` now carry a V3 banner (field-name translation is a migration task); the runtime-resilience fog gains the `429`/`X-RateLimit` requirement. No new ticket.","labels":["wayfinder:ticket","wayfinder:research","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T21:32:59.309Z","closeReason":"Build on V3. V3 is live/public/CORS/no-auth (verified); V2 deprecated (no sunset). Delta mechanical (embed.html/source.url/attachment.url unchanged). V3 publishes rate limits (guest 30/min + headers). docs/research/arena-api-v3.md"}
{"id":"binder-e3d6","title":"Sketch a wireframe-storyboard of the Binder × Are.na experience","status":"closed","type":"task","priority":1,"createdAt":"2026-07-11T19:23:53.473Z","updatedAt":"2026-07-11T19:49:44.054Z","description":"## Question\n\nMake the reimagined Binder concrete: a cheap, low-fidelity **wireframe-storyboard** — a sequence of rough screens/states — of the whole Are.na-driven experience, as something to react to *now*, before the deeper design decisions harden.\n\nCover the journey end to end:\n- **Landing** — first load: a channel resolved into a nav, with title/about drawn from the channel.\n- **Browsing the nav** — how entries read; how nested channels / connections surface (drill-in? tree? sections?).\n- **Selecting block types** — the frame/inline state for a Link (iframe), a Media embed, an Image, a Text block, an Attachment (PDF).\n- **The non-iframeable fallback** — what a Link that refuses framing looks like.\n- **The signature UX** — whether the draggable movable menu and box-shadow aesthetic survive; mobile.\n\nDeliver rough frames (ASCII/box wireframes, an outline, or a stub) as a linked asset via `/prototype`.\n\nScope note: this is the connective, whole-experience sketch — it *feeds* the rigorous per-area tickets *Design the channel-to-navigation organizing model* (`binder-7ac4`) and *Decide per-block embedding and the non-iframeable fallback* (`binder-ab29`); it is not a duplicate of them. Grounded in the verified API facts (`docs/research/arena-public-api.md`).\n\n## Answer\n\nFull storyboard (9 ASCII frames): **`docs/design/binder-arena-storyboard.md`**.\n\nExplored 3 directions in parallel and judged them; chose **\"Same Box, Deeper Stack\"** (41/45) as the spine, grafting the best of the other two:\n\n- **Spine — preserve both signatures.** The floating **draggable** double-box-shadow monospace menu *and* the **full-viewport background** stay. One `GET /channels/:slug` resolves the whole site (title→header, description→about, blocks→a numbered index). A **per-class renderer** fills the single background slot: Link→iframe `source.url`, Media→injected `embed.html` (embedly, framing-safe), Image→`<img>`, Text→`content_html`, Attachment→PDF in-frame. Nested channels **drill in place** with a breadcrumb.\n- **Grafts:** a **connections band** (channel↔channel cross-links — the \"third leg\" the block-tree flattens) and an **optional Board** grid for large channels (both from the Are.na-native direction); the calm **numbered index**, an explicit **loading/resilience frame**, and a **`link!`** pre-warning glyph (from the reader-first direction).\n- **Correctness fix:** active row is **BLACK** (the live `.active` rule), not red.\n\n**Two forks handed to `binder-7ac4`:** (1) fold logo/`.about` into the panel vs keep the corner wordmark; (2) offer the opt-in Board view or stay list-only.\n\n**Risks carried forward:** framing detection is heuristic (denylist + timeout → `binder-ab29`); injected `embed.html` is a third-party-HTML trust surface (→ `binder-ab29`); deep/cyclic drill needs a depth cap + cycle guard (→ `binder-7ac4`); `generated_title` can be \"Untitled\"/truncated; runtime dependence on the deprecated V2 API (→ `binder-d4d4`).\n\nDoes not resolve `binder-7ac4` or `binder-ab29` — it *pre-stages* their decisions with a concrete artifact to react to.","labels":["wayfinder:ticket","wayfinder:prototype","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T19:49:44.054Z","closeReason":"Storyboard delivered: docs/design/binder-arena-storyboard.md. Chose 'Same Box, Deeper Stack' (preserve draggable box-shadow menu + full-viewport slot; per-class renderer; breadcrumb drill; connections band; opt-in Board; fallback card). Pre-stages binder-7ac4 & binder-ab29."}
{"id":"binder-5299","title":"Design the self-hosting config: site meta, channel list & theming","status":"closed","type":"task","priority":2,"createdAt":"2026-07-11T20:44:11.528Z","updatedAt":"2026-07-11T21:48:36.621Z","description":"## Question\n\nDesign Binder's self-hosting **config**: how a self-hoster declares their site and themes it. The organizing model settled that a site = an optional title/about + an **ordered list of Are.na channel slugs** (the sections). Decide:\n\n- **Shape & delivery** — a fetched `config.json` (runtime, matches the no-build data path) vs URL params (`?channels=a,b,c`) vs a small build-time config. Given runtime-fetch + static hosting, `config.json` is the leaning default.\n- **Theming** — what a self-hoster may restyle: is the signature box-shadow / monospace identity fixed, or exposed as overridable tokens? How?\n- **Migration** — this replaces today's `info.json` (`title` / `logo` / `about` + a `menu` of name→URL). Specify the path from that shape to channel-slug sections.\n\nUse `/grilling`.\n\n## Answer\n\nDecided: **`docs/design/config.md`**.\n\n- **Delivery:** a **runtime-fetched `config.json`** next to `index.html` (edit-and-reload, no rebuild) + a `?channel=slug` / `?channels=a,b,c` **URL override** for a zero-config / shareable deploy. Precedence: URL params → `config.json.channels` → a \"configure me\" empty state (never a crash).\n- **Schema:** `{ title?, about?, logo?, channels[] (required — the sections), theme? }`. `channels` accepts slugs or full are.na URLs. **No auth/token field** (consistent with public-only / no-secrets).\n- **Theming:** the **signature look is the default, fully tokenized** (your pick). CSS custom properties (`--binder-panel-bg`, `--binder-shadow-1/2`, `--binder-border`, `--binder-font`, `--binder-text`, `--binder-accent`) live in a **global** stylesheet (Svelte scoped styles don't reach `{@html}`), defaulting to the classic box-shadow/monospace; `config.theme` overrides any subset for a full restyle.\n- **Migration:** `config.json` supersedes `info.json`; `title`/`about`/`logo` carry over; `menu` (name→URL) → `channels` (slugs) is a **content-model shift** — the self-hoster puts content into an Are.na channel (old URLs → Link blocks) and lists the slug(s). Documented; no auto-converter.\n\nNo new ticket. Feeds the codebase-migration fog (config loading + the `info.json`→`config.json` cutover).","labels":["wayfinder:ticket","wayfinder:grilling","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T21:48:36.621Z","closeReason":"Config decided (docs/design/config.md): runtime config.json + ?channel override; schema {title,about,logo,channels[],theme}; signature look default + tokenized via CSS vars; info.json->config.json content-model migration."}
{"id":"binder-c8c3","title":"Name the rebuilt tool — keep \"Binder\" or rename?","status":"closed","type":"task","priority":2,"createdAt":"2026-07-11T21:48:36.740Z","updatedAt":"2026-07-11T22:02:21.395Z","description":"## Question\n\nShould the modernized, Are.na-backed rebuild keep the name **\"Binder\"** or take a **new name**? If renaming: brainstorm candidates fitting the binding/collecting/connecting metaphor + an Are.na-native feel; short, memorable; with an available domain + GitHub repo. Consider continuity (the `clementvalla/binder` repo, the binder-clip metaphor) vs a fresh identity.\n\n## Answer\n\nChosen: **AreNotebook** (Are.na + notebook). Detail: **`docs/design/name.md`**.\n\n- **Availability verified fully clear** (2026-07-11): npm `arenotebook` free, `github.com/arenotebook` free, `arenotebook.com` + `arenotebook.io` both unregistered.\n- **Flagged caveat (not blocking, trivially reversible):** the name leans on the Are.na brand (association / trademark risk) and parses a little awkwardly spoken — worth a \"for Are.na\" framing or a quick trademark sanity-check before the rebrand propagates.\n- **Considered:** keep Binder (continuity default); Sheaf / Weft / Vitrine / Tessle — from a 32-candidate brainstorm across four lenses, availability-vetted.\n\nRebrand (repo / docs / `binder-clip` assets) rides with the codebase migration.","labels":["wayfinder:ticket","wayfinder:grilling","wf-map:binder-9030"],"assignee":"Micah","closedAt":"2026-07-11T22:02:21.395Z","closeReason":"Name: AreNotebook (Are.na + notebook). Availability fully clear (npm+gh-org+.com/.io free). Caveat flagged (brand-association/trademark, reversible). docs/design/name.md"}
